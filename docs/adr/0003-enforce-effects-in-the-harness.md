# Enforce Effects in the Harness

The agent must register a validated Action Plan before publication, and the harness owns every externally visible Effect after checking its capability against the active Authorization Envelope. Prompts remain semantic guidance rather than a permission boundary; SDK callback tools preserve the single warmed run while the harness keeps credentials, idempotency, and policy enforcement.
